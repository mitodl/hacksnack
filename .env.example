# Copy to .env.local for local dev. Only used by the dev server (dev/main.tsx).
#
# Google Maps JS API key for Street View ("map") puzzles. Without it, map
# puzzles that use lat/lng coordinates render without the panorama; all other
# puzzle types work fine.
VITE_GOOGLE_MAPS_API_KEY=
